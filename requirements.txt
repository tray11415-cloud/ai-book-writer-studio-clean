# ---------------------------------------------------------------------------
# Runtime dependencies (required to run the studio and the legacy pipeline)
# ---------------------------------------------------------------------------

# NOTE: the legacy AutoGen multi-agent generator (main.py / agents.py /
# book_generator.py) is NOT installed here, because `autogen` does not yet
# support Python 3.14 and would abort the whole install. The Gradio studio does
# not need it. If you specifically want the legacy pipeline, run:
#     pip install -r requirements-legacy.txt
# on a Python 3.10-3.13 environment.

# OpenAI-compatible client used by the studio, web chat, and compat proxy.
openai>=1.30.0

# .env loading (config.py, app_gradio.py, web_app.py, compat_proxy.py).
python-dotenv>=0.19.0

# HTTP client used by the compatibility proxy (compat_proxy.py).
requests>=2.31.0

# Flask chat UI (web_app.py).
Flask>=3.0.0

# Gradio studio (app_gradio.py). Pin to 4.x as the app targets gradio 4.44.
gradio>=4.44.0,<5.0.0

# Pulled in / relied on alongside gradio at runtime.
fastapi==0.115.2
starlette==0.38.6
huggingface_hub<1.0

# Progress bars and HTML parsing used by the reference/import helpers.
tqdm>=4.65.0
beautifulsoup4>=4.12.3

# ---------------------------------------------------------------------------
# Optional: development tools only (NOT required to run the app).
# Install with:  pip install -r requirements.txt   then they are present,
# or comment this block out for a lean runtime-only environment.
# ---------------------------------------------------------------------------
pytest>=7.0.0   # tests
black>=22.0.0   # code formatting
flake8>=4.0.0   # linting
mypy>=0.910     # type checking
